namespace MyPo.Shared.EF;
public sealed class Globals
{
	public const string TABLE_PREFIX_IDENTITY = "identity_"; // Prefix for all Identity Framework tables in the database, change if needed.

	public const string TABLE_PREFIX = "mypo_"; // Prefix for all tables in the database, change if needed.
}
