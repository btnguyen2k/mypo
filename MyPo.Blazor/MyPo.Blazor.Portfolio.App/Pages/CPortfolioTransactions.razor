@using MyPo.Blazor.App.Shared
@using MyPo.Portfolio.Shared.Api

@inherits BaseComponent

<div class="container mt-3">
	<button class="btn btn-primary btn-sm mb-3" @onclick="BtnClickAddTx"><CIcon icon="cil-plus" /> Add Transaction</button>
	<table class="table table-sm table-bordered border-primary table-striped table-hover align-middle">
		<thead>
			<tr>
				<th scope="col" class="text-center">Type</th>
				<th scope="col" class="text-center">Time</th>
				<th scope="col" class="text-center">Item</th>
				<th scope="col" class="text-center">Quantity</th>
				<th scope="col" class="text-center">Price</th>
				<th scope="col" class="text-center">Tx Fee</th>
				<th scope="col" class="text-center">Tax</th>
				<th scope="col" class="text-center">Cost (other)</th>
			</tr>
		</thead>
		@if (Transactions == null || Transactions.Count() == 0)
		{
			<tbody>
				<tr>
					<td colspan="8" class="text-center">No data found.</td>
				</tr>
			</tbody>
		}
	</table>
</div>

<CModal @ref="@ModalDialogAddTx" title="Record A New Transaction" centered="false" type="primary" size="extra-large">
	<div class="card">
		<div class="card-body">
			<form>
				<div class="row mb-3 align-items-center">
					<div class="col-4">
						<div class="form-floating">
							<select class="form-select" id="txType" aria-label="Buy/Sell" @bind="TxType">
								<option value="Buy">Buy</option>
								<option value="Sell">Sell</option>
							</select>
							<label for="txType">Buy/Sell:</label>
						</div>
					</div>
					<div class="col-4">
						<div class="form-floating">
							<select class="form-select" id="txMarket" aria-label="Market" @bind="TxMarket">
								@foreach (var m in Markets ?? [])
								{
									<option value="@m.Id">@m.Code (@m.Name - @m.Country)</option>
								}
							</select>
							<label for="txMarket">Market:</label>
						</div>
					</div>
					<div class="col-4">
						<div>
							<label for="txTime">Transaction Time:</label>
							<div class="input-group input-group-sm">
								<input type="text" class="form-control" id="txTime" placeholder="Transaction Time" value="@TxTime" data-datetime-picker="true"/>
								<span class="input-group-text"><CIcon icon="calendar"/></span>
							</div>
						</div>
					</div>
				</div>
				<div class="row mb-3 align-items-center">
					<div class="col-3">
						<div class="form-floating">
							<select class="form-select" id="txItemType" aria-label="Item Type" @bind="TxItemType">
								<option value="Stock">Stock</option>
							</select>
							<label for="txType">Item Type:</label>
						</div>
					</div>
					<div class="col-3">
						<div class="form-floating">
							<input type="text" class="form-control" id="txItemCode" aria-label="Item Code" value="@TxItemCode">
							<label for="txItemCode">Item Code:</label>
						</div>
					</div>
					<div class="col-3">
						<div class="form-floating">
							<input type="number" min="0.00" class="form-control" id="txPrice" aria-label="Price" value="@TxItemPrice">
							<label for="txPrice">Price:</label>
						</div>
					</div>
					<div class="col-3">
						<div class="form-floating">
							<input type="number" min="0.01" class="form-control" id="txQuantity" aria-label="Quantity" value="@TxQuantity">
							<label for="txQuantity">Quantity:</label>
						</div>
					</div>
				</div>
				<div class="row mb-3 align-items-center">
					<div class="col-4">
						<div class="form-floating">
							<input type="number" min="0.00" class="form-control" id="txFeeTx" aria-label="Transaction Fee" value="@FeeTx">
							<label for="txFeeTx">Transaction Fee:</label>
						</div>
					</div>
					<div class="col-4">
						<div class="form-floating">
							<input type="number" min="0.00" class="form-control" id="txFeeTax" aria-label="Tax" value="@FeeTax">
							<label for="txFeeTax">Tax:</label>
						</div>
					</div>
					<div class="col-4">
						<div class="form-floating">
							<input type="number" min="0.00" class="form-control" id="txFeeOther" aria-label="Tax" value="@FeeOther">
							<label for="txFeeOther">Tax:</label>
						</div>
					</div>
				</div>
				<div class="row mb-3 align-items-center">
					<div class="col-12">
						<div class="form-floating">
							<input type="text" class="form-control" id="txNotes" aria-label="Notes" value="@TxNotes">
							<label for="txNotes">Notes:</label>
						</div>
					</div>
				</div>
			</form>
			<hr />
			@* <button type="button" class="btn btn-secondary" @onclick="() => ModalDialogDelete.Close()"><CIcon icon="x-circle" /> Cancel</button> *@
			@* <button type="button" class="btn btn-danger" @onclick="() => BtnClickDeleteConfirm()"><CIcon icon="x" /> Delete</button> *@
		</div>
	</div>
</CModal>

@code {
}
