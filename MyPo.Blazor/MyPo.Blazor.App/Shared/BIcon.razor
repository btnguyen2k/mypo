@namespace MyPo.Blazor.App.Shared

<!--
Razor component to render Bootstrap icons. Ref: https://icons.getbootstrap.com/

Sample usage:
    <BIcon icon="bi-alarm"/> or <BIcon icon="alarm"/>

Custom class & style:
    <BIcon icon="bi-alarm" style="font-size: 2rem; color: cornflowerblue;" extraClassName="big"/>
-->

<i class="@IconClass" style="@IconStyle"></i>

@code {
    [Parameter]
    public string icon { get; set; } = string.Empty;

    [Parameter]
    public string style { get; set; } = string.Empty;

    [Parameter]
    public string extraClassName { get; set; } = string.Empty;

    private string IconClass
    {
        get => $"bi bi-{icon.Replace("bi-", "")} {extraClassName}";
    }

    private string? IconStyle
    {
        get => string.IsNullOrWhiteSpace(style) ? string.Empty : style;
    }
}
